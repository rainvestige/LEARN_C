#include <stdio.h>
#define concatenate(A, B) A ## B

int main()
{
  int concatenate(num, 1) = 1;
  int concatenate(num, 2) = 2;
  int concatenate(num, 3) = 3;
  printf("%d%d%d\n", num1, num2, num3);
  return 0;
}
